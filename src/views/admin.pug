doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Admin | Members Only
  body 
    main 
      div 
        for user in users 
          p #{user.username}
      div 
        for msg in messages 
          p #{msg.body}